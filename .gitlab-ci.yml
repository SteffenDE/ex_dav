image: hexpm/elixir:1.11.4-erlang-24.0-alpine-3.13.3

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

mix:
  script:
    - mix test --cover
  coverage: '/(\d+\.\d+%) \| Total/'
